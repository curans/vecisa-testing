---
- hosts: webservers
  user: root

  roles:
    role: Test

  tasks:

  #Instalación de paquetes

  - name: Check installed packages 
    
    yum:
     list: --all 
     state: installed 
    register: mylist

  - name: debug
    debug:
      msg: "{{mylist}}"i

  - name: 
